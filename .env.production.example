# Copy to .env.production (frontend) and fill values. Do NOT commit real secrets.
# Used by the frontend build (Vite expects VITE_ prefix for client-side envs).

# API base used by the frontend to proxy /api requests or point to your backend
VITE_API_URL=https://your-api.example.com
